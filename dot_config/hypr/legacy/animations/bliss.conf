animations {
    enabled = yes
    bezier = bliss, 0.05, 0.9, 0.1, 1
    animation = windows, 1, 5, bliss, slide bottom
    animation = windowsOut, 1, 3, bliss, slide bottom
    animation = border, 1, 10, bliss
    animation = borderangle, 1, 8, bliss
    animation = fade, 1, 7, bliss
    animation = workspaces, 1, 5, bliss
    # Rofi
    bezier = rofiFast, 0.05, 0.9, 0.1, 1
    animation = layersIn, 1, 2, rofiFast, fadeIn
    animation = layersOut, 1, 1, rofiFast, fadeOut
}
